{
  "name": "country-music-preview",
  "version": "1.0.0",
  "private": true,
  "description": "Static Cloudflare Pages preview for Donna Block's country music feature writing site.",
  "scripts": {
    "preview": "npx wrangler pages dev public --local",
    "deploy": "npx wrangler pages deploy public --project-name=country-music-preview"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
